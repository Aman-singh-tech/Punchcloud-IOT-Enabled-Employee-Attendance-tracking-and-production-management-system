{
  "name": "punchcloud",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:backend": "pnpm --filter backend start:dev",
    "dev:admin": "pnpm --filter admin-dashboard dev",
    "dev:self-service": "pnpm --filter self-service dev",
    "test:backend": "pnpm --filter backend test",
    "build": "pnpm -r build"
  }
}
